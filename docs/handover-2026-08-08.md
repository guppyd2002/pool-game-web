# Handover — 2026-08-08 (stop point)

**Audience:** next session (or anyone who was not in the room today).  
**Read time:** ~5 minutes.  
**Goal:** do not make the wrong decision. If unclear after this file, stop and ask fleet — do not invent numbers or green tests.

---

## 1. Where is the code?

| Line | SHA (short) | Role |
|------|-------------|------|
| **`master` / `origin/master`** | `11638b7` | **Production.** Vercel Git auto-deploys Production from `master`. Live site was verified on this SHA 2026-08-08. |
| **Feature (only active line)** | `prep/shot-clock-off-and-deploy-gate` → tip after this handover commit | All unmerged work for today. **Pushed. Not merged.** |

### Why master is frozen

- “Freeze hash” while Production = `master` is meaningless: any push to master ships.
- CEO has **not** decided: (1) shot clock stay/kill, (2) deploy gate.
- **Until both are decided: do not `git push origin master`, do not merge this feature branch to master, do not deploy / change Vercel.**

### What is on the feature branch (vs `11638b7`) — four commits

| Commit | What | Runtime impact if merged |
|--------|------|---------------------------|
| `94dcf53` | Shot-clock **kill-switch** `WALL_CLOCK_SHOT_TIMER_ENABLED=false` + `docs/shot-clock-off-prep.md` + `docs/deploy-gate-options.md` | HUD 30s clock never starts; timeout foul path stays in rules but is not triggered from wall clock. |
| `7b7620c` | **DIV-008 (b)** — BIH null placement = Unity in-place shot (no head-spot respot) | **Behaviour change** on HVA / demo / headless / AI re-derive. |
| `7debce3` | Pin tests: HVA BIH place/null; **HS-002 intentionally left red** | Test-only + honesty. |
| `7725b90` (+ this handover) | Seed-batch runner + three-rulers README notes | **Test-infra only.** Zero product behaviour. |

**Also on feature (same commits):** `ai-bih-inplace-shot.test.ts`, HVA mechanism asserts, shot-timer flag tests.

**Intentionally not changed:**

| Item | Why leave alone |
|------|-----------------|
| `replay-controller` Mode A (`cueBallPlaced else respot`) | **Record playback** of saved positions — not the AI-null path. Do not “align” it to (b). |
| Master / Vercel / Production | CEO gates. |
| HS-002 absolute numbers | Wait QA (卡卡西) on post-(b) ruler B — see §2. |

---

## 2. Intentionally red tests — do not “fix” them green

### HS-002 (headless-game.test.ts) — **RED by policy**

```
it('HS-002: STALE respot-ON asymmetric zero-cap …')
  expect(caps).toBe(0);   // WILL FAIL after (b)
```

**Why red is correct**

1. First goldens (`d371b76`) measured under **swapped** `calculateAIShot` args → false AI.  
2. Second goldens (`72c54f5` / `11638b7`) claimed asymmetric headless **cap === 0** while product still **respotted** null BIH — that is a kinder world, not SP-004.  
3. After `7b7620c`, headless is no-respot; observed ~35% cap (N=20) on seed+shot r4v2 — absolute zero is dead.

**Anyone who wants green MUST, in order:**

1. Wait for **卡卡西 / QA** measurement on **committed post-(b) tree** (`7b7620c` or later tip), **ruler B only** (`*7919`, SP-004 style / runner `--harness sp004`).  
2. Get **directional** bands (e.g. symmetric cap-hit **>** asymmetric cap-hit), **not** a magic percent and **not** a single-seed fate.  
3. Rewrite the assertion to that mechanism (and change **ruler** if needed).  
4. **Forbidden:** lower `0` to `7` (or any number you measured casually) just to pass CI.

**HS-002b** (sym r4v4 SP-004 completion not 100%) is a **directional sanity** that should stay green on this branch — it is not the stale zero-cap claim.

### Pin tests that should be green on feature

- `ai-bih-inplace-shot.test.ts` — (b) mechanism  
- HVA BIH place / null / forceShot pins in `human-vs-ai.test.ts`  
- `shot-timer` flag + disabled-start tests  

If those are red, the branch is broken — fix or stop. If only HS-002 is red, that is expected.

---

## 3. Two CEO decisions — after they answer, do this

Full design text: `docs/shot-clock-off-prep.md`, `docs/deploy-gate-options.md`.  
Below is **execution**, not debate.

### Decision 1 — Shot clock: 撤 (off) vs 留 (on)

| CEO says | Action |
|----------|--------|
| **撤 (kill wall-clock)** | Keep `WALL_CLOCK_SHOT_TIMER_ENABLED = false` (already on feature). Ship **only after** deploy gate is safe (Decision 2). Merge path depends on gate (below). Smoke: idle HotSeat ≥35s → still P1 turn, no countdown, no BIH handoff. |
| **留 (keep 30s)** | Do **not** merge kill-switch as-is. Either leave master alone, or merge only non-timer commits, or set flag **`true`** before any ship. Live prod today still has the 30s clock (`11638b7`). |

Flag location: `src/renderer/shot-timer.ts` → `export const WALL_CLOCK_SHOT_TIMER_ENABLED`.

### Decision 2 — Deploy gate (so “freeze” means something)

| CEO picks | First concrete steps (no guessing) |
|-----------|-------------------------------------|
| **A — Production branch = `release`** (leaning rec) | Vercel → Project → Settings → Git → **Production Branch = `release`**. One-time: `git branch release 11638b7 && git push origin release` (or current prod SHA). Protect `release`. Ship later by PR into `release`, **not** by pushing master. |
| **B — Manual `vercel --prod`** | Disable Production auto-deploy from Git. Ship only: clean checkout of known SHA + `vercel --prod`. Never from dirty tree. |
| **C — Tag / GitHub Release** | Production auto-deploy off for branch pushes; CI deploys on `release: published` / `v*` tag only. |

**After gate exists**, shipping feature work is a **deliberate promote**, not “push master”.  
**Before gate exists**, merging (b) or kill-switch to master **immediately changes live CEO product**.

### Suggested order after both answers

1. Install gate (Decision 2) **first** if any ship is planned.  
2. Then promote only the commits CEO wants (timer and/or (b) and/or docs/runner).  
3. Cherry-pick is fine if CEO wants only kill-switch without (b), or only (b) without kill-switch.  
4. Re-smoke: idle 35s (if clock off); one HVA BIH null path (if (b) shipped).

---

## 4. Three rulers — post-DIV-008(b) truth

Canonical long form: `tests/fixtures/ai-quality/README.md` (updated with this stop).

| Ruler | BIH when AI returns `cueBallNewPos === null` | Seed formula (typical) | Status after (b) on **feature** |
|-------|-----------------------------------------------|------------------------|----------------------------------|
| **A — Product paths** | **In-place shot** (no respot) — HVA, demo, headless, AI re-derive | HVA AI: local `*7919`; headless/demo: `seed+shot` | **(b) aligned A with Unity on null placement.** Still **not** the same as B (seed / seat stream differ). |
| **B — SP-004 harness** | No respot (always was) | `seed + globalShot * 7919` both seats | **Control group.** N=50 r4v4: completion **58%**, cap **42%**, completed-only foul median **0.1** @ measure `7debce3`. Unchanged class vs N=20 ~55%/45% → (b) did not “fix” SP-004 by accident. |
| **C — Unity C#** | Keep transform, then Shot() | C# seed stream | GT for placement fidelity. |

**Mode A record playback** still uses `cueBallPlaced else respot` — that is **recorded positions**, not live AI null. Do not fold it into A/B/C placement debates.

**Do not:**

- Cite B completion % as “product quality for CEO.”  
- Compare sp004 runner numbers to headless seed+shot as one table.  
- Claim master still has respot for AI null — **master does**; **feature does not** on live AI paths.

Master (`11638b7`) product still respots null BIH. Feature (`7b7620c+`) does not. That is the main behaviour delta waiting on gate + CEO.

---

## 5. Measurement pipeline (use this, not in-process N=50)

In-process multi-seed Vitest OOMs (physics frames on one heap). Permanent tool:

```bash
# Ruler B / SP-004, rank 4v4, seeds 0..49, resume-safe
node scripts/run-seed-batch.mjs \
  --harness sp004 --rank0 4 --rank1 4 --n 50 \
  --out /tmp/sp004-r4-n50.json \
  --partial-dir /tmp/seed-partials-sp004-r4
```

- Child heap: `--max-old-space-size=8192` (or `SEED_BATCH_HEAP_MB`).  
- Output: Kakashi-shaped JSON with **`measureCommit`**.  
- Headless formula: `--harness headless` (different ruler — do not merge tables).  
- Details: `tests/fixtures/ai-quality/README.md` § Seed-batch runner.

Verified 2026-08-08: N=50 full run **no OOM**; `--partial-dir` resume works.

---

## 6. Hard rules for the next person

1. **No push master. No merge to master. No Vercel / prod deploy** until CEO answers both items in §3.  
2. **Do not green HS-002** without QA bands on ruler B post-(b).  
3. **Do not “fix” Mode A respot** in `createPlaybackController` as part of DIV-008.  
4. **Every AI number:** first line = which ruler + seed formula + measure commit SHA.  
5. Prefer **new work on this feature branch** (or a new branch from it); do not accumulate more behaviour surface until CEO returns.  
6. If you only remember one sentence: **measurement credibility is the ceiling of every conclusion — runner exists so we never substitute code-path inference for a number we could not run.**

---

## 7. Quick pointers

| Need | Path |
|------|------|
| Shot clock OFF prep | `docs/shot-clock-off-prep.md` |
| Deploy gate A/B/C | `docs/deploy-gate-options.md` |
| Three rulers + runner | `tests/fixtures/ai-quality/README.md` |
| Kakashi baseline (pre-(b) measure) | `tests/fixtures/ai-quality/kakashi-foul-metric-baseline-6a5f87d.*` |
| (b) product code | `human-vs-ai.ts`, `ai-demo.ts`, `headless-game.ts`, `replay-controller` AI re-derive only |
| Intentionally red HS-002 | `src/__tests__/game/headless-game.test.ts` |

**Stop.** No further feature work required for this freeze window.
